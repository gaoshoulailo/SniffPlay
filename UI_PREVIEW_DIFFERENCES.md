# SniffPlay 界面效果稿差异清单

## 对照范围

- 效果稿：[`ui-next-stage-preview.html`](ui-next-stage-preview.html)
- 当前界面：`src/sniffplay_ui` 下的 QML 页面与组件
- 对照日期：2026-09-25

本文记录当前项目与效果稿仍然存在的差异。效果稿中的静态示例数据不等同于项目已经具备的数据能力，因此差异分为“纯界面调整”和“需要数据或业务支持”两类。

## 当前完成情况

以下方向已经基本实现：

- 顶部显示“音乐库 / 当前位置”。
- 歌单页使用左侧歌单、右侧歌曲详情的主从布局。
- 正在播放页采用开放式封面布局，并加强歌曲信息层级。
- 正在播放页具有“播放队列 / 歌词”分段切换。
- 队列歌曲具有封面、当前播放状态和悬停操作。
- 底部播放器在所有主要页面显示。
- 搜索输入框具有搜索图标、清空按钮和焦点效果。

## 1. 应用框架与侧边栏

| 项目 | HTML 效果稿 | 当前项目 | 差异性质 |
| --- | --- | --- | --- |
| 默认窗口尺寸 | 约 `1260 x 820` | `1180 x 760` | 纯界面调整 |
| 最小窗口宽度 | 可缩小到移动端宽度 | 最小宽度 `920` | 框架调整 |
| 侧边栏响应式 | 小于 `900px` 收成图标栏 | 固定宽度 `214px` | 尚未实现 |
| 品牌图标 | 蓝底音频波形图标 | 蓝底字母 `S` | 纯界面调整 |
| 导航顺序 | 正在播放、搜索、我的歌单、我的收藏、播放历史 | 正在播放、搜索、我的收藏、我的歌单、播放历史 | 纯界面调整 |
| 常用歌单 | 侧边栏展示三个快捷歌单 | 没有快捷歌单区 | 需要接入歌单模型 |
| 底部账户区 | 头像、音乐库名称、数据源和在线状态 | Bilibili 数据源状态卡片 | 产品结构不同 |

相关实现：`src/sniffplay_ui/Main.qml`。

## 2. 顶部标题栏

当前位置提示已经接近效果稿，但仍有以下区别：

- 当前项目在歌单页会进一步显示选中的歌单名称，效果稿只显示页面名称。
- 当前标题栏高度为 `42px`，效果稿约为 `43px`。
- 当前项目保留设置、最小化、最大化和关闭按钮的真实窗口行为。
- 效果稿在窄屏隐藏部分窗口按钮，当前项目不支持这一断点。

这部分差异较小，不影响主要界面层级。

## 3. 正在播放页

### 已经接近效果稿的部分

- 左侧不再使用明显的外层卡片边框。
- 封面具有颜色阴影和悬停反馈。
- 歌名、歌手、专辑和来源已经分层展示。
- 主控制区包含随机、上一首、播放、下一首和循环。
- 右侧具有播放队列与歌词切换。
- 队列行具有封面缩略图和悬停操作按钮。

### 仍然不同的部分

| 项目 | HTML 效果稿 | 当前项目 | 原因 |
| --- | --- | --- | --- |
| 封面光晕 | 使用封面副本进行模糊扩散 | 使用当前歌曲强调色阴影 | Qt 实现方式不同 |
| 封面角标 | `HQ 320K` | “正在播放 / 已暂停” | 当前没有音质字段 |
| 标题副文案 | 来自具体歌单，如“夜间漫游” | 来自当前播放队列 | 缺少当前队列来源字段 |
| 元数据 | 来源、年份、播放量 | 来源、队列位置、时长、专辑 | 当前模型没有年份和播放量 |
| 队列摘要 | 歌曲数量和总时长 | 只有歌曲数量 | 未计算队列总时长 |
| 歌词内容 | 展示演示歌词和当前行高亮 | 展示“暂无歌词”空状态 | 项目没有歌词数据源 |
| 左右分隔 | 右侧有淡色竖线 | 仅通过间距区分 | 纯界面调整 |

相关实现：`src/sniffplay_ui/pages/NowPlayingPage.qml`。

## 4. 底部播放器

底部播放器已经显示在正在播放页，但结构仍未完全一致。

| 项目 | HTML 效果稿 | 当前项目 |
| --- | --- | --- |
| 定位方式 | 绝对定位，悬浮覆盖在页面内容上方 | 位于主界面的纵向布局中，会占用页面高度 |
| 高度 | `96px` | `92px` |
| 中央控制 | 随机、上一首、播放、下一首、循环 | 收藏、上一首、播放、下一首、单曲循环 |
| 右侧控制 | 队列按钮、音量按钮、音量条 | 音量图标和音量条 |
| 进度信息 | 顶部只突出进度线 | 同时显示当前位置和总时长 |
| 对齐方式 | 三列网格，播放按钮严格居中 | 单行布局加弹性间距，视觉上近似居中 |
| 背景模糊 | 对播放器后方页面内容取样 | 主要对应用背景图取样 |

相关实现：`src/sniffplay_ui/components/PlayerBar.qml` 和 `src/sniffplay_ui/Main.qml`。

## 5. 搜索页

搜索页是当前项目与效果稿差异最大的页面。

| 项目 | HTML 效果稿 | 当前项目 | 差异性质 |
| --- | --- | --- | --- |
| 主布局 | 全宽高密度歌曲列表 | 左侧发现卡片、右侧结果列表 | 结构不同 |
| 排序 | 相关度、时长、歌名 | 没有排序菜单 | 需要排序逻辑 |
| 结果行高度 | `60px` | `72px` | 纯界面调整 |
| 专辑和来源列 | 桌面端同时显示 | 会因右侧空间不足而隐藏 | 受双栏布局影响 |
| 结果统计 | 位于页面标题说明中 | 位于结果列表标题右侧 | 纯界面调整 |
| 页面辅助操作 | 刷新 | 随机播放和刷新 | 功能取舍不同 |
| 当前歌曲展示 | 不单独占用大卡片 | 左侧发现卡片展示当前歌曲 | 当前项目特有设计 |

当前搜索框本身已经基本匹配效果稿，包括图标、清空按钮、焦点边框和本地文件按钮。

相关实现：`src/sniffplay_ui/pages/SearchPage.qml`。

## 6. 歌单页

主从结构已经实现，但歌单详情头部仍明显不同。

| 项目 | HTML 效果稿 | 当前项目 | 差异性质 |
| --- | --- | --- | --- |
| 页面标题 | 位于整个页面上方 | 位于左侧歌单栏顶部 | 纯界面调整 |
| 新建歌单 | 带文字的主按钮 | 只有加号图标 | 纯界面调整 |
| 歌单项高度 | `52px` | `62px` | 纯界面调整 |
| 歌单图标 | 不同颜色和图标 | 统一蓝色音乐图标 | 可由界面生成，也可存储配置 |
| 歌单详情横幅 | 封面、描述、名称和播放按钮 | 名称、歌曲数和操作按钮 | 尚未实现 |
| 歌单封面 | `110 x 110` 图片 | 没有 | 需要歌单封面字段 |
| 歌单描述 | 展示场景描述 | 没有 | 需要歌单描述字段 |
| 歌曲操作 | 播放和收藏为主 | 播放、上移、下移、删除 | 当前项目更偏管理功能 |

相关实现：`src/sniffplay_ui/pages/PlaylistPage.qml`。

## 7. 我的收藏

效果稿将收藏页设计成全宽高密度列表，当前项目仍保留双栏展示：

- 当前桌面端左侧有大型封面卡片，右侧是收藏歌曲列表。
- 当前标题为“喜爱”，效果稿为“我的收藏”。
- 当前收藏行不显示封面，效果稿歌曲行显示小封面。
- 当前行内按钮持续占用布局宽度，效果稿主要在悬停时显示操作。
- 当前紧凑模式会隐藏左侧卡片，但应用外壳最小宽度仍限制了窄屏效果。

相关实现：`src/sniffplay_ui/pages/FavoritesPage.qml`。

## 8. 播放历史

播放历史整体比其他页面更接近效果稿，但数据组织不同：

- 当前项目真实显示专辑、时长和播放时间。
- 效果稿说明文案提到“按日期分组”，但演示内容实际没有完成日期分组。
- 当前项目也尚未按“今天、昨天、更早”进行分组。
- 当前操作区和列宽比效果稿更偏桌面数据表。
- 当前没有针对窄屏逐级隐藏专辑、时间等次要列。

相关实现：`src/sniffplay_ui/pages/HistoryPage.qml`。

## 9. 空状态与反馈

当前项目在这部分比效果稿更完整：

- 搜索、收藏、歌单、队列均有真实空状态。
- 空状态包含图标、说明和可执行按钮。
- 收藏、歌单和播放操作会触发统一 Toast。
- 右键菜单和创建歌单弹窗已经使用统一视觉组件。

效果稿只展示了主要正常状态，没有完整覆盖这些业务状态，因此这里不建议为了完全一致而删减当前实现。

## 10. 响应式布局

效果稿包含两个完整断点：

- 小于 `900px`：侧边栏收成图标栏，双栏页面变成单栏。
- 小于 `600px`：搜索工具栏分行，播放器减少控制项，列表继续压缩。

当前项目的情况：

- 应用窗口最小宽度是 `920px`。
- 侧边栏始终保持 `214px`。
- 搜索页和歌单页使用 `760px` 内部紧凑阈值。
- 正在播放页使用 `650px` 内部紧凑阈值。
- 页面内部虽然具有紧凑模式，但在当前最小窗口宽度和固定侧边栏下，部分阈值很难真正触发。

若要达到效果稿的窄屏表现，需要先改造应用外壳和侧边栏，而不只是调整单个页面。

## 11. 需要数据层支持的差异

以下内容不能只修改 QML：

| 功能 | 所需支持 |
| --- | --- |
| 真实歌词和逐行高亮 | 歌词来源、时间轴模型、播放位置同步 |
| `HQ 320K` 音质标识 | 播放资源码率或音质信息 |
| 歌曲年份 | Track 模型增加发行年份 |
| 播放量 | Provider 返回并保存播放统计 |
| 当前歌曲来自哪个歌单 | 播放队列保存来源上下文 |
| 歌单封面 | 数据库与歌单模型增加封面字段 |
| 歌单描述 | 数据库与歌单模型增加描述字段 |
| 搜索结果排序 | 控制器或代理模型提供排序规则 |
| 队列总时长 | 控制器聚合队列歌曲时长 |

## 12. 建议实施顺序

1. 将搜索页改成效果稿的全宽高密度列表，这是当前最明显的结构差异。
2. 补齐歌单详情横幅；若暂不扩展数据库，可先使用自动生成的拼贴封面和本地默认描述。
3. 将收藏页统一为全宽歌曲列表，减少不同页面之间的结构跳变。
4. 调整底部播放器为真正覆盖内容的悬浮层，并重新校准页面底部安全间距。
5. 改造侧边栏和最小窗口宽度，补齐 `900px` 以下的响应式布局。
6. 最后再评估歌词、音质、年份、播放量和歌单元数据等数据层能力。

## 结论

当前项目已经实现效果稿的核心视觉语言，包括深色界面、蓝色强调、统一图标、悬停反馈、顶部位置提示、歌单主从布局和新版正在播放页。剩余差异主要不在颜色，而在搜索页、收藏页、歌单详情横幅、播放器定位方式和应用级响应式结构。

其中搜索页、收藏页、播放器定位和响应式侧边栏可以直接通过 QML 调整；歌词、音质、年份、播放量、歌单封面与描述则需要扩展 Python 控制器、模型或数据库。
